// ── Trick Bone Glow ────────────────────────────────────────────────────────
// The verdigris fire-glow effect is now CSS-only: the `trickBoneFlameGlow`
// keyframe animation is applied to every [data-trick-glow] element directly
// in the stylesheet, matching the same drop-shadow pattern used by the card
// challenge glows (claimGlowGreen / claimGlowRed).  No canvas overlay is needed.

export function initTrickBoneGlow() {
  // No-op: effect is handled by the trickBoneFlameGlow CSS animation.
}
